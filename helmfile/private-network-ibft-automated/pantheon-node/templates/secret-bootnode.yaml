{{- if and .Values.bootnode.enabled .Values.bootnode.privKey -}}
{{- $fullname := (include "pantheon-node.fullname" .) -}}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "pantheon-node.fullname" . }}-keys
  labels:
    app.kubernetes.io/name: {{ include "pantheon-node.name" . }}-keys
    helm.sh/chart: {{ include "pantheon-node.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
data:
  private.key: |-
    {{ .Values.bootnode.privKey | b64enc }}

{{- end -}}