apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "quorum.fullname" . }}-prometheus-configmap
  labels:
    app: {{ template "quorum.fullname" . }}-prometheus-configmap
    chart: {{ template "quorum.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    namespace: monitoring
  namespace: monitoring
data:
  prometheus.yml: |-
{{ .Files.Get "prometheus.yml" | indent 4}}