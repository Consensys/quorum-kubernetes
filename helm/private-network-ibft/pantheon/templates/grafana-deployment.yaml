---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "pantheon.fullname" . }}-grafana
  labels:
    app: {{ template "pantheon.name" . }}
    chart: {{ template "pantheon.chart" . }}
    release: {{ .Release.Name }}
    namespace: monitoring
    heritage: {{ .Release.Service }}
    component: grafana
  namespace: monitoring
spec:
  replicas: 1
  selector:
    matchLabels:
      component: grafana
  template:
    metadata:
      labels:
        app: {{ template "pantheon.name" . }}
        release: {{ .Release.Name }}
        component: grafana
    spec:
      containers:
        - name: grafana
          image: grafana/grafana:6.2.5
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          env:
            - name: POD_IP
              valueFrom:
                fieldRef:
                  fieldPath: status.podIP
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: GF_SECURITY_ADMIN_USER
              value: "admin"
            - name: GF_SECURITY_ADMIN_PASSWORD
              value: "admin"
          volumeMounts:
            - name: grafana-configmap-datasources
              mountPath: /etc/grafana/provisioning/datasources/prometheus.yml
              subPath: prometheus.yml
              readOnly: true
            - name: grafana-configmap-dashboards-dashboard
              mountPath: /etc/grafana/provisioning/dashboards/dashboard.yml
              subPath: dashboard.yml
              readOnly: true
            - name: grafana-configmap-dashboards-pantheon
              mountPath: /etc/grafana/provisioning/dashboards/pantheon.json
              subPath: pantheon.json
              readOnly: true
          ports:
            - containerPort: 3000
              name: http
              protocol: TCP
      volumes:
        - name: grafana-configmap-datasources
          configMap:
            name: pantheon-grafana-configmap-datasources
        - name: grafana-configmap-dashboards-dashboard
          configMap:
            name: pantheon-grafana-configmap-dashboards-dashboard
        - name: grafana-configmap-dashboards-pantheon
          configMap:
            name: pantheon-grafana-configmap-dashboards-pantheon
